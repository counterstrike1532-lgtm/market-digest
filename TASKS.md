# TASKS.md

Читай `CLAUDE.md` перед началом, особенно раздел «ГРАБЛИ».
Делай по одной задаче. После каждой — стоп, показывай diff и что проверить.

---

## ГОТОВО (не переделывай)

- ~~T1 Прополка источников~~ — 20 из 21 фида живы. Мёртвые заменены реальными адресами
  GUS и запросами Google News. Eurostat RSS отдаёт 0 записей и это нормально, он не нужен:
  данные Eurostat берутся через API в `numbers.py`.
- ~~Кодировки, PowerShell-обёртки, падения Gemini, схлопывание дублей, догрузка статей~~ —
  см. «ГРАБЛИ» в CLAUDE.md.

---

## T2 — Настройка качества отбора (делать первой)

Владелец запустит `.\run.ps1 dry` и покажет вывод. Твоя задача — по живому результату
довести `RANK_PROMPT` и `DRAFT_PROMPT` в `src/brain.py`.

На что смотреть в выводе:

1. **Разброс оценок.** Если снова всё сваливается в 7-8 — усиль требование разброса.
   Вариант: требовать ровно одну оценку 9+ на батч, или заставить ранжировать внутри батча
   и выдавать позицию, а не абсолютный балл.
2. **Структуры черновиков.** Проверь, что три черновика реально разные (SHAPE A/B/C) и не
   начинаются одинаково. Если модель всё равно шаблонит — разнеси генерацию на три
   отдельных вызова, по одной структуре в каждом.
3. **Поле FIGURES.** Каждое число должно иметь источник. Если модель пишет числа при
   `SOURCE TEXT: (unavailable)` — правило нарушено, усиливай запрет.
4. **Хэштеги.** Если лезут генерик-теги (#finance, #GPW, #macroeconomics) — запрети совсем.
5. **Мусор в сюжетах.** Что прошло отбор, но по мнению владельца шлак — добавь в список
   критериев отбраковки его словами.

Не переписывай промпты целиком «чтобы стало лучше». Меняй адресно и объясняй каждую правку.

---

## T3 — Коллектор X/Twitter

Владелец согласен рисковать **отдельным** аккаунтом X. Не основным.

### Ключевое архитектурное решение

Работает **локально, не в GitHub Actions.** X агрессивно флагует IP дата-центров: с раннера
GitHub сессия проживёт дни, с домашнего IP — недели или месяцы.

```
локально (Task Scheduler, 1-2 раза в день)
  src/collect_x.py -> state/x_latest.json -> git commit && push
основной прогон
  src/main.py читает state/x_latest.json, если он свежий (< 20 часов)
```

Нет файла или он старый — дайджест собирается без X, без ошибки. Обязательно.

### Реализация

- Playwright, в отдельный `requirements-local.txt` (чтобы Actions его не тянул)
- `python -m src.collect_x --login` — видимый браузер, владелец логинится руками,
  сессия в `.secrets/x_state.json`. Проверь, что путь в `.gitignore`
- обычный запуск headless, восстанавливает сессию, идёт на **List**, не в домашнюю ленту:
  список детерминированный и курируемый. URL списка — в `config/sources.yaml`,
  новая секция `twitter: {list_url: ..., min_faves: 30, max_tweets: 60}`
- скроллит до `max_tweets` или до твитов старше 24 часов
- забирает: автор, текст, ссылка, лайки, ретвиты, время
- фильтр: отбросить ретвиты, ответы и всё ниже `min_faves`
- конвертирует в `Item` из `src/collect.py` (`tag="x"`, `weight=0.9`, `social=лайки`),
  сериализует в `state/x_latest.json` с полем `collected_at`
- человеческие паузы `random.uniform(1.5, 4.0)`, окно 1280x900, реалистичный UA
- селекторы не нашлись — внятная ошибка «разметка X изменилась», а не тихое падение

Отдельно `docs/setup_x.md`: как создать burner-аккаунт, собрать List, поставить
Task Scheduler на Windows, что делать когда сессия отвалится.

---

## T4 — Bluesky (риска ноль)

Официальный публичный API, бесплатный. Часть finance/AI людей туда дублирует.

`src/collect_bluesky.py`: `com.atproto.server.createSession` (env `BSKY_HANDLE` /
`BSKY_APP_PASSWORD`), затем `app.bsky.feed.getAuthorFeed` по списку хендлов из конфига.
Только `requests`, без SDK. Отдаёт `Item` с `tag="bluesky"`.

Живёт в основном пайплайне, Actions не мешает.

---

## T5 — Скорер черновиков

`src/score_draft.py` — CLI: подаёшь текст поста, получаешь отчёт перед публикацией.

Без LLM: длина 110-170 слов; есть хотя бы одно число; нет запрещённых слов из
`DRAFT_PROMPT`; нет engagement-bait в конце; хэштегов 0-1; нет ссылок в теле;
не открывается риторическим вопросом.

Через Gemini: есть ли личная ставка или это пересказ; **не в вопросительной ли позиции**
написан пост (главный флаг); звучит как студент или как консультант; пройдёт ли
за человеческий текст.

Вывод PASS/FAIL по пунктам плюс 2-3 конкретные правки. Пост за владельца не переписывать.

---

## T6 — Перенос на GitHub Actions

Воркфлоу `.github/workflows/digest.yml` уже написан, но не проверен в бою.
Когда локальные прогоны стабильны: создать **публичный** репо, залить секреты
(`GEMINI_API_KEY`, `GEMINI_MODEL`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`),
прогнать вручную через workflow_dispatch, убедиться что `state/seen.json` коммитится обратно.

Playwright и коллектор X в Actions не тащить.

---

## T7 — Локальный дашборд (по желанию)

`--html` в `src/main.py`: пишет `out/digest_YYYY-MM-DD.html`. Тёмная тема, сюжеты со
ссылками, цифры таблицей, черновики в блоках с кнопкой копирования. Один файл,
без CDN и внешних зависимостей.

---

## T8 — графики и дайджест-посты (ЖДЁТ ПРИЁМКИ ВЛАДЕЛЬЦА)

Реализовано по `docs/TZ_T8.md`, все четыре части, все четыре коммита сделаны отдельно
(T8a/T8b/T8c/T8d). Не переделывай без результата боевого прогона — см. пункт
«на ручную приёмку» в отчёте, который был выдан по завершении задачи.

- T8a: `src/chartstyle.py` + `src/charts.py` (market_overview, figures_chart), `numbers.py`
  отдаёт ряды stooq отдельным ключом `_series`, `deliver.send_photo`, интеграция в
  `main.py` (`--no-charts`), `matplotlib` в requirements.txt.
- T8b: `DRAFT_PROMPT` теперь выдаёт фиксированные 3 роли (digest 120-170 слов,
  digest-short 80-110, single) вместо трёх произвольных постов, добавлен TONE CEILING.
- T8c: `RANK_PROMPT` — критерий польской аудитории; `DRAFT_PROMPT` — VERDICT/WHY/CHECK_FIRST
  на каждый черновик независимо.
- T8d: правило закрывающего вопроса (не в каждом посте, приглашение экспертизы, не
  валидации); `src/verify.py` — верификатор цифр (уровень a без LLM + уровень b, 1 запрос
  на весь прогон при наличии проверяемых черновиков), подключён в `main.py`.

Юнит-тесты на моках прогнаны (парсинг черновиков, польский формат чисел, NOT_FOUND/
NO_SOURCE_TEXT/MISMATCH, сохранность исходного текста черновика). Живой `--dry`/`send`
не запускался — на это и указывает статус «ждёт приёмки».

---

## T9 — инфраструктура качества + рыночные данные (ЖДЁТ ПРИЁМКИ ВЛАДЕЛЬЦА)

Реализовано по ТЗ T9, все шесть частей, каждая своим коммитом (T9a-T9f). Промпты
RANK/DRAFT/SCORE не тронуты. 67 pytest-тестов зелёные, все на моках, без сети и без
Gemini. Живой `--dry`/`send` не запускался — приёмка за владельцем.

- T9a: `tests/` + `requirements-dev.txt` (pytest, в боевой requirements.txt не идёт),
  `.github/workflows/tests.yml` (push/PR, `digest.yml` не тронут). Регрессионные тесты
  на реальных строках из прогонов. Попутно: `parse_figures` не распознавал значения
  с единицей измерения ("$200 billion", "100,000 PLN") — регэксп расширен.
- T9b: верификатор считал знаменатель по числу РАСПОЗНАННЫХ чисел, а не по числу пар в
  FIGURES — черновик с 3 числами, из которых распарсилось одно, показывал обманчивый
  чистый "✅ verified (1/1 found)". Новые статусы UNPARSED (единица измерения не даёт
  привести к числу) и YEAR (голый год, законно не в знаменателе).
- T9c: stooq окончательно мёртв (антибот требует JS, живой прогон подтвердил — все
  три символа отдают HTML-заглушку). Каскад на yfinance для WIG20/S&P/Nasdaq, формат
  вывода не меняется. WIG20.WA у Yahoo почти без истории — используется
  ETFBW20TR.WA (ETF на WIG20TR), подтверждено живым прогоном. `yfinance` добавлен
  в requirements.txt.
- T9d: `state/metrics.json` — запись на каждый боевой (не `--dry`) прогон: стадии
  воронки, вердикты С УЧЁТОМ понижения верификатором, статусы цифр, расход квоты
  Gemini, какая модель писала черновики, какой источник отдал рынки. 90 дней,
  коммитится вместе с `seen.json` в `digest.yml`. Визуализации нет и не планируется.
- T9e: домен вместо полного URL в ссылках (иначе `news.google.com/rss/articles/...`
  на 300+ символов делал сводку нечитаемой), деление на куски по 3900 больше не режет
  URL пополам, дедуп заголовка "DRAFT n" (было "DRAFT 2DRAFT 2 (A)"), сборка сводки
  через явный `"\n\n".join` вместо хрупкого трюка с ведущим `\n` — гарантирует пустую
  строку между сюжетами независимо от содержимого полей.
- T9f: `figures_chart` рисовал график по 7 числам из 3 разных сюжетов сразу
  (диджест-черновик тянет 2-3 сюжета в одном FIGURES), с подписями-именами
  источников вместо показателей. Теперь: не рисуется, если SOURCE черновика
  перечисляет больше одной ссылки, ИЛИ один источник процитирован дважды, ИЛИ
  подпись похожа на цитату ("report", "article", "Source [3]").

### На ручную приёмку

1. `.\run.ps1 dry` — глазами посмотреть, что сводка в консоли читается (домены
   вместо голых URL, пустые строки между сюжетами, ни один "DRAFT n" не задвоен).
2. Если ключ Gemini под рукой — прогнать реальный черновик с FIGURES, где есть число
   с единицей измерения (например "$200 billion"), и проверить строку "ЦИФРЫ: ..."
   под черновиком: должна честно показывать unparsed, не выдавать себя за полную проверку.
3. `.\run.ps1 send` (или `--dry` без `--no-charts`) — посмотреть, рисуется ли график
   рынков (сейчас всегда через yfinance, stooq мёртв) и не рисуется ли график по
   FIGURES диджест-черновика (там почти всегда несколько сюжетов).
4. После первого боевого прогона — глянуть `state/metrics.json`, что запись похожа на
   правду (числа по стадиям воронки не бессмысленные).
